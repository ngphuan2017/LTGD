@model IEnumerable<BTTH2.Models.Author>

@{
    ViewBag.Title = "Index";
}

<h2>Tác giả</h2>

<p>
    @Html.ActionLink("Thêm tác giả", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AuthorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AuthorName)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.AuthorId }) |
            @Html.ActionLink("Xem chi tiết", "Details", new { id=item.AuthorId }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.AuthorId })
        </td>
    </tr>
}

</table>
